/** Shared motion tokens so every animation stays consistent and lives in one place. */
export const EASE: [number, number, number, number] = [0.4, 0, 0.2, 1]
export const DUR = { fast: 0.15, base: 0.2 } as const
